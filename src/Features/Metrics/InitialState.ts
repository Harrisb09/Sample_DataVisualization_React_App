import { IMetricsState } from "../../interfaces/IMetricsState";

export const initialState: IMetricsState = {
    metrics: [],
    graphData: {},
    measurementData: {},
    selectedMetrics: [],
  };